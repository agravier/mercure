(ns mercure.agent
  (:use [mercure.core] :reload)
  (:import [mercure.core Fleet Planet]))

(defn agent
  "Function called by the game loop. receives a countdown function."
  [countdown-f]
  )

(defn -main [& args]
  )
